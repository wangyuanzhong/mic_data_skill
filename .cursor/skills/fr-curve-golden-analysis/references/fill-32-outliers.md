# 步骤3.2：奇异值分析（版式对照，提名待确认）

由 `run_curves.py`（`exclude` 为 `null`）写入 `## 奇异值分析`。Agent **不要手填**；对照本文件检查。  
数字来自 `outlier_review` / `curve_meta`。

```markdown
## 奇异值分析

- 分析频段：{f_lo}–{f_hi} Hz
- Q 豁免阈值：Q ≥ {q_threshold}
- 提名门槛：max|dev| ≥ max(1.0 dB, 2×批次中位数)
- 门禁状态：pending（待用户确认）
- 脚本建议剔除：{suggested 或 无}
- 高Q豁免（不建议剔除）：{exempt 或 无}

### 提名明细（来自 outlier_review）

| 样机 | maxAbsDev | 频率Hz | Q | Q稳定 | 高Q豁免 | 建议剔除 | 理由 |
|------|-----------|--------|---|-------|---------|----------|------|
| … | … | … | … | … | … | … | … |

### 用户决定

待确认。请回复：不剔除 / 按建议剔除 / 自定义名单（样机名逗号分隔）。
```
