# 步骤2：md 日志 + 标准化核对

先改好同目录 `params.json`（真源），再在 `## 标准化与脚本入参` 写日志与核对：

```markdown
### 脚本入参
见同目录 `params.json`（本 md 不传参）。

### 标准化说明
已生成 `process.xlsx`（sheet 名：curves）。纵轴频率，横轴第1行为样机名，以下为幅度。

### 标准化核对
| 项 | 映射侧 | process.xlsx | 是否一致 |
|----|--------|--------------|----------|
| 样机数 | … | … | 是/否 |
| 样机名单 | … | … | 是/否 |
| 抽查 | 样机X @约1000Hz = a dB（源） | 同位置 = b dB | 是/否 |
```
